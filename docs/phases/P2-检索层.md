# P2 — 混合检索层（源码级设计）

> 上游：`docs/IMPLEMENTATION-PLAN.md` §P2；架构决策 3/8/10/11/12（BM25+向量 RRF、版本元数据、jieba、as-of 过滤、标题感知切块）
> 评分映射：第二关 20 分（检索质量隐藏题库 + DEBUG_LOG 根因质量）
> 预算：16h　出口：retrieval 15 题显著超基线、DEBUG_LOG ≥12 条闭环

---

## 1. starter 缺陷定位（本阶段要修的，源码行号已坐实）

| # | 位置 | 缺陷 | 证据 |
|---|---|---|---|
| D5 | `service.py:70` | `kb_docs` 数目录文件数（36，含无编号 README），契约要入索引文档数（35） | `sum(1 for path in ...rglob("*"))` |
| D6 | `tokenizer.py:20-22` | `normalise(text).split()` 按空白分词，中文整句成一个 token，BM25 对中文实质失效 | 函数体两行 |
| D7 | `loader.py:12` | `SUPPORTED_SUFFIXES={.md,.markdown}`，.txt/.html 不进索引 | KB-022.txt/KB-061.html/KB-062.txt 丢失 |
| D8 | `loader.py:82-84 decode_bytes()` | 一律 UTF-8 + `errors="ignore"` | KB-062（GBK）乱码 |
| D9 | `loader.py:178-182` | HTML 不剥标签直接入库，注释还声称"BM25 自己会忽略" | KB-061 正文污染 → quote 逐字校验失败 |
| D10 | `chunker.py:41` | `range(0, len-CHUNK_SIZE, CHUNK_SIZE)` 丢每篇文尾 <300 字；定长切在句中 | range 上界写死 |
| D11 | `index.py:23-27` | 缓存键只哈希三个版本常量，不含知识库内容；`.cache/index.json` 已提交（P0 已删文件，本阶段修函数） | `content_key(kb_dir)` 不用 kb_dir |
| D12 | `retriever.py:275-276` | `hit.doc_id = ordered[len(hits)].doc_id` 张冠李戴：返回的 doc_id 是排序第 n 篇文档而非该 chunk 所属 | 行 276 原文 |
| D13 | `retriever.py:306-307` | 取满 top_k **之后**再滤已废止版本，契约明令"先过滤后截取" | 行 307 列表推导 |
| D14 | `sessions.py:16-28` | `_turns` 全局单链表，`history(session_id)` 忽略 session_id | P3 修，此处登记 |

> 可移植复用（不要重写）：`aliases.py` 全模块（别名表解析、跨语言桥 `distinctive_tokens`、`strict_mentions` 归一入索引）；`sanitize.py`（注入句正则）；`retriever.py` 的 `_eligible/_multiplier/_concept_scores/_coverage` 打分框架（修好 D12/D13 后结构是对的）。

## 2. 新核心文件设计（kbqa/core/）

### 2.1 `core/textnorm.py` — 可见正文管线（与评测脚本同构）

**直接复刻 `eval/run_eval.py` 的 `decode_bytes` / `html_to_text` / `normalize_doc` 三个函数**（逐字复制 + 注释注明出处行号），这是 quote 逐字校验（`quotes_verbatim`，每轮必查）不红的唯一稳妥办法：

```python
def decode_bytes(raw: bytes) -> str:
    """UTF-8 严格解码；失败降级 GB18030（KB-062）。与评测脚本行为一致。"""
def html_to_text(html: str) -> str:
    """剥标签 + &amp; 等实体反转义，保留连续正文（KB-061）。与评测脚本行为一致。"""
def visible_text(raw_text: str, fmt: str) -> str:
    """文档入库前的统一形态：fmt=html 先 html_to_text。chunk 从这里切。"""
def normalize_doc(text: str) -> str:
    """评测同款规范化：NFKC → 去全部空白（含零宽空格）→ 去 * ` | # >。
    只用于 quote 校验与长度计算，不入库、不影响检索打分。"""
```

### 2.2 `core/loader.py` — 四后缀加载器（替换 loader.py）

```python
SUPPORTED_SUFFIXES = {".md", ".markdown", ".txt", ".html"}     # 修 D7

@dataclass
class Document:   # 字段在 starter Document 基础上加两个
    ...            # doc_id/title/text/path/fmt/doc_type/status/effective_from/
                   # superseded_by/stores/stores_explicit/updated_at/warnings
    meta_source: str = "frontmatter"   # frontmatter / body_inferred / defaulted（记 trace，B6）
    encoding: str = "utf-8"            # 实际解码方式（记 trace）

def load_document(path: Path) -> Optional[Document]:
    # 1) decode_bytes（core.textnorm 版，修 D8）
    # 2) fmt==html → html_to_text（修 D9）；fmt==md → parse_front_matter（移植 loader.py:87）
    # 3) doc_id：frontmatter doc_id → 文件名 ^KB-\d+ → None（跳过+warning，kb_docs 自然=35）
    # 4) 元数据降级链（B6）：frontmatter 缺 status → "现行"；缺 effective_from →
    #    正文 _EFFECTIVE_PATTERNS（移植 loader.py:120）→ 邮件 Date: 头 → 任意日期 → None（自始有效）
    #    每一步把 meta_source 记下
def load_knowledge_base(kb_dir) -> tuple[list[Document], list[str]]  # 修 D7 后缀过滤
```

### 2.3 `core/chunker.py` — 标题层级感知切块（替换 chunker.py，修 D10）

```python
CHUNKER_VERSION = "chunker-3"     # 缓存键成分
MIN_CHARS, TARGET_MAX = 200, 500

def chunk_document(document: Document) -> list[Chunk]:
    """1) 按标题行（^#{1,6} / 全角编号行）切段，每段带 heading 路径
       2) 段 > TARGET_MAX：按段落（空行）二切，仍超则按句切（sanitize.split_sentences）
       3) 段 < MIN_CHARS：与上一段合并（同 heading 层级优先）
       4) chunk.text 逐字保留原文；chunk_id = f"{doc_id}#{n}"
       5) 全覆盖不变式：''.join(chunk 原文) == document 可见正文（测试断言）"""
```

### 2.4 `core/tokenizer.py` — jieba 分词（替换 tokenizer.py，修 D6）

```python
import jieba
TOKENIZER_VERSION = "tokenizer-3"

def init_jieba(alias_table) -> None:
    """rebuild 时把 KB-003 别名（含数据库写法）jieba.add_word，保证
    '牛肉poke'/'味增拉面'/'Makai Poke' 不被切碎。"""

def normalise(text: str) -> str:   # 保留 starter 语义（NFKC + lower）
def tokenize(text: str) -> list[str]:
    """jieba.lcut(normalise(text))，去纯标点/空白 token。"""
def content_tokens(text: str) -> list[str]:   # 保留 starter STOP_CHARS/STOP_WORDS 逻辑
```

### 2.5 `core/index.py` — BM25 索引 + 缓存键（修 D11）

移植 `index.py` 的 `BM25Index`（postings/idf/score_terms/coverage 数学不变），只改：

```python
def content_key(kb_dir: Path) -> str:
    """修 D11：缓存键 = sha256( 版本常量 + 每个知识库文件的(相对路径, size, mtime_ns, 内容sha1) )。
    换知识库必失效；索引产物存 var/index.json（不再跟仓库走）。"""
```

### 2.6 `core/vector.py` — 向量侧（可选，自动降级）

```python
MODEL_NAME = "paraphrase-multilingual-MiniLM-L12-v2"   # ~470MB，README 写明首次下载体积
class VectorIndex:
    @classmethod
    def build(cls, chunks, db_path) -> "VectorIndex":
        """sentence-transformers 计算 embedding → 存 var/vector.db（chunk_id, blob float32）。
        依赖缺失/下载失败 → cls.unavailable()，检索自动降级纯 BM25（契约 7.6）。"""
    def scores(self, query: str, allowed: set[int]) -> dict[int, float]: ...
```

### 2.7 `core/retriever.py` — 融合检索（修 D12/D13）

```python
RRF_K = 60
def rrf_fuse(bm25: dict[int, float], vec: dict[int, float]) -> dict[int, float]:
    """{pos: Σ 1/(k+rank)}；向量不可用退化为 BM25 单路。"""

class Retriever:
    """移植 starter Retriever 的打分框架（_eligible/_multiplier/_concept_scores/
    _history_factor/coverage 全部保留——这些逻辑是对的），修两处："""
    def search(self, query, top_k=5, as_of=None, ...) -> SearchResult:
        # 修 D13：_eligible 过滤在打分前完成（allowed = 合格文档的 chunk 集合），
        #         从 allowed 内取满 top_k，禁止取完再滤
        # 修 D12：删除 retriever.py:276 那行覆写；Hit.doc_id 恒等于 chunk.doc_id
        # 新增 B6 排序先验：_multiplier 加 NOTICE_BOOST=1.2
        #   doc_type ∈ {通知,政策,调整} 时 ×1.2（KB-062 23:00 压 KB-042 21:30）
        # 新增：Hit.dropped_instructions —— 进 LLM 上下文前对 hit.text 跑
        #   sanitize.sanitize()，被剥句子记入（检索打分与 quote 仍用原文）
```

### 2.8 `core/quote.py` — quote 提取（为 P3 citations 准备，本阶段先建 + 自测）

```python
def shortest_verbatim_quote(doc_text: str, fact_hint: str, limit=400) -> Optional[str]:
    """从可见正文中截取含 fact_hint 关键词的最短连续句段（split_sentences 粒度拼接），
    normalize_doc(quote) 长度 ≤ limit。返回 None 表示截不出——调用方不得引用。"""
def check_verbatim(doc_text: str, quote: str) -> bool:
    """评测同款判定：normalize_doc(quote) in normalize_doc(doc_text)。"""
```

### 2.9 rebuild 接线（core/rebuild_all.py 或改 kbqa/rebuild.py）

`python -m kbqa.rebuild` 改为依次：`build_clean_db → load_knowledge_base → init_jieba → chunk → BM25Index → VectorIndex.build → 写 var/`，打印 cleaning report、文档数（35）、chunk 数、缓存键、全部 warnings。`health().kb_docs = len(index.docs_meta)`（修 D5）。

## 3. TDD 红测试清单

**红→绿**：`tests/defects/test_d05~d13_*.py` 先对老代码红，提交 `test: 复现检索层缺陷 D5–D13（红）`；新核心逐层转绿后按缺陷分组提交 `fix:`。

| 文件 | 测试 | 断言 |
|---|---|---|
| defects/test_d05_health.py | `test_kb_docs_35` | /api/health kb_docs == 35（== 入索引文档数） |
| defects/test_d06_tokenizer.py | `test_chinese_tokens` | tokenize("外卖订单多久内可以退款") 含"外卖"/"退款" 等 ≥4 个词，而非 1 个整句 token |
| defects/test_d07_suffixes.py | `test_txt_html_indexed` | KB-022/KB-061/KB-062 均在索引 docs_meta |
| defects/test_d08_gbk.py | `test_kb062_no_mojibake` | KB-062 正文含"23:00"且无 U+FFFD/乱码 |
| defects/test_d09_html.py | `test_kb061_stripped` | KB-061 全 chunk 无 `<` 标签残留；含实体原文（& 而非 &amp;） |
| defects/test_d10_chunker.py | `test_full_coverage` | 每篇文档：chunks 拼接 == 可见正文（文尾不丢） |
| | `test_size_band` | 90% chunk 在 200~500 字（表格密集文档允许例外，记 warning） |
| defects/test_d11_cache.py | `test_key_follows_kb` | 改任一 KB 文件 → content_key 变化（P0 已红，此处转绿） |
| defects/test_d12_docid.py | `test_hit_docid_owns_chunk` | 任意查询：每个 hit.doc_id == hit.chunk_id 的 `#` 前缀 |
| defects/test_d13_topk.py | `test_exactly_topk_after_filter` | 构造含已废止文档的查询：results 仍 == top_k 条且全部合格 |
| test_retriever.py | `test_asof_filters_deprecated` | as_of=2026-09-01 滤 KB-010 v1；as_of=2026-06-30 不滤 |
| | `test_archived_not_filtered` | status=归档 的文档不参与过滤 |
| | `test_notice_beats_master` | "Super Souper 周五营业到几点" top1 为 KB-062 系（23:00），非 KB-042（21:30） |
| | `test_crosslingual_salmon` | "salmon" 查询命中英文邮件 KB-022 且中文查询"三文鱼"也能命中它 |
| | `test_injection_stripped` | KB-060 检索命中时 dropped_instructions 含注入句，hit.text 原文不变 |
| | `test_alias_doc_penalty` | KB-003 别名词典本身不占据 top1（ALIAS_DOC_PENALTY 生效） |
| | `test_retrieve_never_500` | VectorIndex.unavailable 时 /api/retrieve 正常返回 |
| test_quote.py | `test_verbatim_eval_compatible` | 对 10 组 (doc, quote) 跑 check_verbatim，含 GBK/HTML/全角数字样本 |
| | `test_quote_length_cap` | 截取的 quote normalize 后 ≤400 |

## 4. 实施步骤（按序）

1. 红测试 D5–D13 落地确认红 → commit `test: 复现检索层缺陷 D5–D13（红）`。
2. `core/textnorm.py`（复刻评测三函数）+ `test_quote.py` → commit。
3. `core/loader.py` → D7/D8/D9 转绿 → commit `fix: D7–D9 加载器四后缀/GBK/HTML`。
4. `core/chunker.py` → D10 转绿 → commit `fix: D10 标题感知切块不丢文尾`。
5. `core/tokenizer.py`（jieba + 别名词典挂载）→ D6 转绿 → commit `fix: D6 jieba 中文分词`。
6. `core/index.py` 缓存键 → D11 转绿 → commit `fix: D11 缓存键含知识库内容哈希`。
7. `core/retriever.py`（移植打分框架 + 修 D12/D13 + NOTICE_BOOST + 注入剥离）→ D12/D13 转绿 → commit `fix: D12 doc_id 归位 / D13 先滤后取`。
8. `core/vector.py` + RRF（向量不可用降级）→ commit `feat: BM25+向量 RRF 融合`。
9. rebuild 接线 + health.kb_docs → D5 转绿 → commit `fix: D5 kb_docs 口径`。
10. `/api/retrieve` 切到新 Retriever（与问答链路同一实例，契约硬性要求）。
11. 每完成一层跑公开评测观察 retrieval 类 15 题爬升；DEBUG_LOG 逐条回填（D5–D13 共 9 条 + P1 的 4 条 = 13 条 ≥ 12 达标）。
12. 换库冒烟：临时改一篇 KB 文档 → rebuild → 检索结果跟着变（零写死证据）。

## 5. 出口检查单

- [ ] retrieval 类 15 题得分接近满分，R03/R04/R05（营业时间/英文邮件/GBK）全绿
- [ ] DEBUG_LOG 累计 ≥12 条闭环（每条含红测试名 + 修复 commit）
- [ ] `kb_docs=35`、`kb_chunks` 合理（标题感知切块后远多于 starter 的定长块数）
- [ ] 缓存随知识库内容失效；向量不可用降级不 500
- [ ] EVAL_REPORT.md 记录本阶段得分 + commit
