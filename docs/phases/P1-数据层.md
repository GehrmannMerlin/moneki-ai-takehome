# P1 — 清洗层 + 口径引擎 + metrics API（源码级设计）

> 上游：`docs/IMPLEMENTATION-PLAN.md` §P1；架构决策 5（规范化全量表 + 行级双口径剔除标记）
> 评分映射：第一关 12 分（能跑 + 指标 API 与口径一致）
> 预算：10h　出口：M01–M06 全对、N01 的 `valid_sales_rows=18290` 绿

---

## 1. starter 缺陷定位（本阶段要修的，源码行号已坐实）

| # | 位置 | 缺陷 | 证据 |
|---|---|---|---|
| D1 | `cleaning.py:77-102 clean_rows()` | 完全不清洗：日期照抄字符串、不去重、不查外键、空 amount/无法解析 amount 按 0 保留、qty 解析失败按 0 | 函数体只有 trim 和 parse，无任何剔除；`valid_sales_rows` 因此=18628 |
| D2 | `tools.py:53 _where()` | `date >= ? AND date < ?` 右开区间，契约要求闭区间 | `"date < ?"` 字面量；start=end 查单日时结果为空 |
| D3 | `tools.py:96-109 query_metrics()` | v2/v3 口径混淆：`is_refund = 0` 排除退款行、`refund_amount` 硬编码 0、`COUNT(*)` 当订单数（应为销售行 DISTINCT order_id）、aov=净额/行数 | SQL 原文 |
| D4 | `tools.py:74-79 run_sql()` | 任意 SQL + `conn.commit()`，连接非只读 | `self.conn.commit()`；S02/S03 安全题与 metrics_unchanged 检查必挂 |

## 2. 新核心文件设计（kbqa/core/）

### 2.1 `core/normalize.py` — 规范化原语（纯函数，无 IO）

```python
"""规范化：编号、日期、金额、数量。全部被清洗层与工具层共用。"""
from datetime import date
from decimal import Decimal, InvalidOperation
from typing import Optional

def norm_id(value: object) -> str:
    """trim + upper。门店/商品/order_id 统一走这里。"""

def parse_date(value: object) -> Optional[date]:
    """三种输入格式：YYYY-MM-DD / YYYY/M/D / DD-MM-YYYY（日在前，KB-001 §2.2）。
    必须过 datetime.date 构造校验——'2026-13-45' 正则合法但日历非法，返回 None。
    返回 None 即触发剔除规则 1。"""

def parse_amount_cents(value: object) -> tuple[Optional[int], str]:
    """移植 cleaning.py:25 parse_amount，语义不变：去 ¥￥/空白 → Decimal → 分。
    返回 (cents, status)，status ∈ {ok, empty, bad}。empty/bad 都触发剔除规则 2（不回填）。"""

def parse_qty(value: object) -> Optional[int]:
    """移植 cleaning.py:40。None 或 ≤0 触发剔除规则 3。"""
```

关键实现细节：`parse_date` 对 `DD-MM-YYYY` 的判定——KB-001 §2.2 明确"日在前"，直接按该格式解析再 `date(y, m, d)` 构造；不要先正则再字符串拼接日期（`'2026-13-45'` 拼出来是非法日期字符串，闭区间比较时会静默漏算且 `valid_sales_rows` 变 18293）。

### 2.2 `core/cleaning.py` — 清洗与建库

**clean.db DDL（替换 `cleaning.py:105 _SCHEMA`）**：

```sql
CREATE TABLE stores (store_id TEXT PRIMARY KEY, store_name TEXT, category TEXT, district TEXT);
CREATE TABLE products (product_id TEXT PRIMARY KEY, product_name TEXT,
                       product_category TEXT, unit_price REAL);
-- 决策 5：规范化全量表 + 行级双口径剔除标记，剔除推迟到计算层
CREATE TABLE clean_lines (
    rowid_pk   INTEGER PRIMARY KEY,
    order_id   TEXT NOT NULL,          -- 已 norm_id
    date       TEXT NOT NULL,          -- 已规范化为 YYYY-MM-DD；无法解析时存 NULL 并标记
    store_id   TEXT NOT NULL,
    product_id TEXT NOT NULL,
    qty        INTEGER,                -- 解析失败存 NULL
    amount_cents INTEGER,              -- empty/bad 存 NULL
    payment    TEXT,
    is_refund  INTEGER NOT NULL,       -- amount_cents < 0
    dup_group  INTEGER,                -- 七字段完全重复的分组号，无重复为 NULL
    reject_v3  TEXT,                   -- 六规则首因（NULL=保留）；取值同 REMOVAL_REASONS
    reject_v2  TEXT                    -- v2 口径的剔除原因（NULL=保留）
);
CREATE INDEX idx_lines_date ON clean_lines(date);
CREATE INDEX idx_lines_store ON clean_lines(store_id);
CREATE INDEX idx_lines_product ON clean_lines(product_id);
CREATE TABLE rejects_audit (           -- 数据质量面板与调试用
    rule TEXT, line_snapshot TEXT
);
CREATE TABLE meta (key TEXT PRIMARY KEY, value TEXT);
```

**六条剔除规则（KB-001 v3 §3，按序首因归因；期望值已独立复算）**：

| 规则 | reject_v3 值 | 判定逻辑 | 期望剔除 |
|---|---|---|---|
| 1 | `1_unparseable_date` | `parse_date` 返回 None | 8（3×'N/A'、2×''、3×'2026-13-45'） |
| 2 | `2_empty_amount` | `parse_amount_cents` status ≠ ok | 150 |
| 3 | `3_qty_le_zero` | qty is None 或 qty ≤ 0 | 30 |
| 4 | `4_store_not_in_stores` | norm_id(store_id) ∉ stores 表 | 10 |
| 5 | `5_product_not_in_products` | norm_id(product_id) ∉ products 表 | 40 |
| 6 | `6_duplicate_row` | 七字段（order_id,date,store_id,product_id,qty,amount,payment）规范化后完全重复，保留组内首行 | 100 |

合计剔除 338，保留 **18290**（18196 销售行 + 94 退款行；amount=0 行实测为 0 行）。退款行**不剔除**（v3 计入净营业额）。

v2 口径标记（`reject_v2`）差异三处：退款行剔除（v2 不计入）、空 amount **回填** qty×unit_price 后不剔除、客单价分母为明细行数（这是计算层差异，不进标记）。

**函数签名**：

```python
@dataclass
class CleaningReport:   # 兼容 starter CleaningReport.as_dict() 形状，扩 v2 字段
    raw_rows: int; kept_rows: int; kept_sales_rows: int; kept_refund_rows: int
    removed: dict[str, int]; note_unparseable_amount: int
    def as_dict(self) -> dict: ...

def clean_rows(rows, stores: set[str], products: set[str]) -> tuple[list[tuple], CleaningReport]
def build_clean_db(source: Path, target: Path) -> CleaningReport   # 替换 cleaning.py:120
```

守恒自验内建在 `build_clean_db`：断言 `kept_rows + sum(removed.values()) == raw_rows`，不等直接 raise（rebuild 炸在早期，不留到评测）。

### 2.3 `core/metrics.py` — 口径引擎（v3 默认 / v2 参数化）

```python
class Caliber(str, Enum): V3 = "v3"; V2 = "v2"

class MetricsEngine:
    """/api/metrics/*、看板、chat data_evidence 共用同一引擎（契约一致性根源）。"""
    def __init__(self, db_path: Path): ...   # 线程局部只读连接，移植 tools.py:30-44
        # 只读模式打开：sqlite3.connect(f"file:{path}?mode=ro", uri=True)

    def _reject_col(self, caliber: Caliber) -> str:   # v3→reject_v3, v2→reject_v2
    def _where(self, start, end, store_id, product_id, caliber) -> tuple[str, list]:
        """闭区间 date >= ? AND date <= ?（修 D2）；且 <reject_col> IS NULL。"""

    def summary(self, start, end, store_id=None, product_id=None,
                caliber=Caliber.V3) -> dict:
        """五指标（KB-001 v3 §4）：
        net_revenue  = SUM(amount_cents) 含退款行（v2: 仅销售行 + 回填行）
        refund_amount= -SUM(amount_cents) WHERE is_refund=1（v2: 恒 0）
        orders       = COUNT(DISTINCT order_id) WHERE is_refund=0（v2: COUNT(*) 明细行数）
        aov          = net/orders，Decimal + ROUND_HALF_UP 2 位（移植 tools.py:22 round2）
        qty          = SUM(销售qty) - SUM(退款qty)（v2: 仅销售）
        空区间：数值 0、aov None，不报错（契约 §2）。"""

    def daily(self, start, end, store_id=None, product_id=None, caliber=Caliber.V3) -> dict:
        """移植 tools.py:122 daily_metrics 的补零逻辑，修闭区间与口径。"""

    def valid_sales_rows(self, caliber=Caliber.V3) -> int:
        """SELECT COUNT(*) FROM clean_lines WHERE <reject_col> IS NULL"""
```

### 2.4 `core/datatools.py` — 只读数据工具（本阶段先 6 个，P3 扩到 11）

```python
class DataTools:
    def __init__(self, engine: MetricsEngine): ...
    query_metrics  = engine.summary            # 同名保持 service.py 兼容
    daily_metrics  = engine.daily
    def payment_mix(self, start, end, store_id=None, caliber=V3) -> dict
        # 移植 tools.py:153，占比算子 share_orders/share_revenue 保留 6 位小数
    def top_products(self, start, end, store_id=None, limit=10, caliber=V3) -> dict
    def by_store(self, start, end, product_id=None, caliber=V3) -> dict
    def by_store_category(self, start, end, caliber=V3) -> dict
    def first_sale_date(self, product_id) -> Optional[str]
    def unit_price_check(self, product_id, start, end, store_id=None) -> dict
    def compare_periods(self, start_a, end_a, start_b, end_b, store_id=None,
                        product_id=None, caliber=V3) -> dict
        # 差额 = 舍入后指标之差（D04 期望 0.17 = 36.53-36.36，必须用 round2 后的值相减）
    # run_sql：本阶段移除；P3 以「只读 SQL 闸」形态重新引入（SELECT/WITH 开头+有 FROM+mode=ro 连接）
```

移植时只改三处口径（D2/D3/D4），其余 SQL 形状保留——starter 这些函数的 GROUP BY/JOIN 结构是对的。

### 2.5 外壳接线（service.py 最小改动）

- `Service.rebuild()`：`build_clean_db` 改指 `core.cleaning`；`self.tools = DataTools(MetricsEngine(settings.clean_db))`。
- `health()`：`valid_sales_rows = engine.valid_sales_rows()`（=18290）；`kb_docs` 本阶段先保持现状，P2 改为入索引文档数（N01 两个字段一起绿）。
- `/api/data_quality`（server.py:108 已有路由）：`cleaning_report` 从新 meta 读，六项剔除计数直接喂前端面板。

## 3. TDD 红测试清单（tests/）

**红→绿工作流**：先写 `tests/defects/test_d01_cleaning.py ~ test_d04_run_sql.py` 对 starter 老代码断言正确行为（红），提交 `test: 复现清洗层缺陷 D1–D4（红）`；新核心落地后同断言转绿，提交 `fix: D1–D4 清洗与口径引擎`。

| 文件 | 测试 | 断言（期望值用 conftest.EXPECTED） |
|---|---|---|
| defects/test_d01_cleaning.py | `test_valid_rows_18290` | build 后 `COUNT(*) WHERE reject_v3 IS NULL` == 18290 |
| | `test_removed_breakdown` | removed == {8,150,30,10,40,100}，合计 338 |
| | `test_conservation` | kept + removed == 18628 |
| | `test_calendar_illegal_rejected` | `'2026-13-45'` 行进 rejects_audit 且 rule=1_unparseable_date |
| | `test_refund_rows_kept` | kept_refund_rows == 94，退款行 reject_v3 IS NULL |
| defects/test_d02_interval.py | `test_closed_interval_single_day` | start=end='2026-06-18' 的 summary 非空且等于该日数据 |
| defects/test_d03_caliber.py | `test_summary_m01/m02/m04` | 五指标逐一 == EXPECTED（含 aov=36.36/47.70/68.40） |
| | `test_orders_distinct` | orders == 销售行 DISTINCT order_id，≠ COUNT(*) |
| | `test_refund_in_net` | net_revenue 含退款行；refund_amount=953.00（M01） |
| defects/test_d04_run_sql.py | `test_run_sql_readonly` | `DROP TABLE`/`UPDATE` 被拒绝或抛错；执行后 summary 不变 |
| test_metrics.py | `test_empty_range_zero` | 空区间数值 0、aov None（M05 考点） |
| | `test_daily_pads_missing` | daily 区间内每天一条，无数据日 0/None |
| | `test_aov_half_up` | 构造 cents/orders 使第三位小数=5，断言进位（HALF_UP 非银行家） |
| | `test_v2_v3_diverge` | 同查询 v2/v3 结果不同且各自符合口径 |
| | `test_compare_delta_rounded` | compare_periods 的 aov 差额 = round2 后之差（36.53−36.36=0.17） |
| test_api_metrics.py | `test_summary_contract_shape` | 响应含契约 §2 全字段；`test_daily_contract_shape` 同理 |
| | `test_health_valid_rows` | /api/health valid_sales_rows == 18290 |

## 4. 实施步骤（按序）

1. 写 defects 红测试 4 个文件 → 跑确认全红 → commit `test: 复现清洗层缺陷 D1–D4（红）`。
2. 实现 `core/normalize.py` + 单测 → commit。
3. 实现 `core/cleaning.py`（含守恒断言）→ D1 转绿 → commit `fix: D1 清洗六规则+日历校验`。
4. 实现 `core/metrics.py` + `core/datatools.py` → D2/D3 转绿 → commit `fix: D2 闭区间 / D3 v3 口径引擎`。
5. service.py 接线 + health 字段 → commit。
6. 移除 run_sql（暂以 `raise NotImplementedError` 占位，P3 重建只读闸）→ D4 转绿 → commit `fix: D4 移除可写 SQL 通道`。
7. 跑公开评测：`python eval/run_eval.py ...`，M 类 6 题应全绿、N01 的 valid_sales_rows 绿（kb_docs 待 P2）；分数与分类分解表对比基线，记 EVAL_REPORT.md。
8. DEBUG_LOG 回填 D1–D4 四条（现象/假设/验证/根因行号/修复 commit/红测试名）。

## 5. 出口检查单

- [ ] 17 条红测试全绿；`make test` 全绿
- [ ] 公开评测 M01–M06 满分、N01 的 18290 命中
- [ ] DEBUG_LOG 新增 4 条闭环记录
- [ ] EVAL_REPORT.md 记录本阶段得分 + commit hash
- [ ] 数据质量接口产出六项剔除计数（P4 前端直接消费）
