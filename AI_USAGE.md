# AI 使用说明

作业要求的三节：用了哪些 AI 工具、怎么拆任务（附真实 prompt 例子）；
AI 给过哪些**错误的**诊断或修复、我怎么发现的；哪些判断是我自己做的、为什么不交给 AI。

**从 P0 起随手记。** 这一项事后补不出来——被 AI 坑的那一刻如果不记，
三天后只能写出"AI 很有帮助"这种没有信息量的话。

---

## 1. 用了哪些 AI 工具，怎么拆任务

| 工具 | 用在什么环节 | 不用于什么 |
|---|---|---|
| Claude Code / DeepSeek Harness（会话式 agent） | 通读 starter 全部源码、逐行定位缺陷、把审查结论落成设计文档、写测试骨架与文档草稿 | 不用于判断口径（口径以 KB-001 v3 原文为准，我逐条对照） |
| 同一个 agent 的"扫描 + 复算"模式 | 从 `pos.db` 独立复算清洗与指标，验证 AI 给出的数字 | 不用于决定"哪个数字是对的"——复算脚本的输出我要自己核 |
| 评测脚本 `eval/run_eval.py` | 唯一的裁判。每次改动后看分数涨跌 | 它是评委给的，不是我写的，所以它说什么就是什么 |

### 拆任务的方式

我按**阶段 + 可验证出口**拆，而不是按"让 AI 写一整个模块"拆：

```
SDD：先把这一阶段的契约/口径写成文档（接口形状、剔除规则、验收阈值）
  → TDD：把契约翻译成会红的测试（fixture 写死合法，代码写死违法）
  → 实现：最小实现让红转绿，不做测试没覆盖的"顺手发挥"
  → 回归：跑公开题库，分数记进 EVAL_REPORT.md
```

给 AI 的每个任务都带三个约束：**要动的文件清单**、**验收断言**、**不许做什么**
（例如"不要把任何数字写进代码逻辑"）。带上"不许做什么"之后，返工率明显下降。

### 一个真实的 prompt 例子

P0 阶段让 AI 写"缓存键"红测试时，我用的 prompt 大意是：

> 任务：为 starter 的 `kbqa/index.py::content_key` 写一条**在当前代码上会红**的测试。
>
> 要求：
> 1. 断言的行为：知识库目录里**任何一个文件**内容变化后，`content_key(kb_dir)` 的返回值必须变。
> 2. 不许改动 `knowledge_base/` 里的任何原文件——把知识库复制到 pytest 的 `tmp_path` 再改副本。
> 3. 为了让测试有意义，同时写一条**反向**测试：monkeypatch 版本号常量后键**必须**变（证明它不是恒定值）。
> 4. 不许把期望的哈希值写死（那不是断言，那是抄答案）。
> 5. 写完后跑一遍 `pytest tests/test_p0_infra.py -q`，把真实输出贴给我，并告诉我哪几条是红的。
>
> 先说你要怎么改，等我确认再动手。

第 5 条（**要真实输出，不要"应该会红"**）是关键：AI 很容易给你一段推理说
"这个测试会红"，但它其实没跑。要求贴输出之后，我在下面第 2 节里就抓到了两次。

---

## 2. AI 给过的错误诊断 / 错误修复，以及我怎么发现的

### 2.1 AI 写的第一版"无绝对路径"测试是假红（P0，2026-09-25）

- **AI 的建议**：写一条测试遍历 `kbqa/*.py` 的字符串字面量，凡是以 `/` 开头的就判为"绝对路径"，报红。
- **它说的结果**："基准目录下应该会红，因为 `config.py` 里有项目路径。"
- **我要求的**：让它真跑一遍并贴输出。
- **真实输出**：

  ```
  FAILED tests/test_p0_infra.py::test_no_hardcoded_abs_path
  E  AssertionError: 源码里出现绝对路径：['config.py:71  /', 'llm.py:59  /chat/completions',
     'server.py:62  /api/health', 'server.py:67  /api/metrics/summary', ...]
  ```

- **问题**：`/api/health`、`/chat/completions` 是 **URL 路径**，不是路径依赖。
  这条测试会永久假红，而且它"红"的原因跟要防的风险（评委换机器跑不起来）毫无关系。
- **我怎么发现的**：读它贴的输出，发现红的全是路由字符串——没有一条是本机路径。
  这正是作业说的"被 AI 骗了没发现才扣分"：如果我只看"测试红了，很好"，就会把它
  当成有效防线提交，然后每次都靠"这条红是已知的"糊过去。
- **修正**：把判据收窄到**真的会导致换机器跑不起来**的三类：Windows 盘符（`D:\`、`C:/`）、
  UNC（`\\server\share`）、落在已知 POSIX 根目录下的路径（`/home`、`/Users`、`/tmp`、`/var`…）。
  URL 路径天然不在其中，不需要额外白名单。
- **教训**：测试的**红**必须指向它要防的那个风险；"红了"本身不是目的。

### 2.2 AI 写的 eval 模块加载方式会 AttributeError（P0，2026-09-25）

- **AI 的建议**：想在测试里复用评测脚本自己的 `normalize_doc` / `check_quote`
  （避免"测试绿了但评测红"），方案是用 `importlib.util.spec_from_file_location`
  把 `eval/run_eval.py` 当模块载入。
- **它说的结果**：没提任何风险。
- **真实输出**：

  ```
  ERROR at setup of test_public_questions_categories_known
    ..\eval\run_eval.py:276: in <module>
      @dataclass
    dataclasses.py:749: in _is_type
      ns = sys.modules.get(cls.__module__).__dict__
  E AttributeError: 'NoneType' object has no attribute '__dict__'
  ```

- **根因**：`run_eval.py` 用了 `from __future__ import annotations`，
  dataclass 解析字符串注解时要回头查 `sys.modules[cls.__module__]`；
  而 `module_from_spec` 出来的模块**没有登记进 `sys.modules`**。
- **我怎么发现的**：pytest 报的是 `ERROR at setup`（夹具挂了）而不是 `FAILED`，
  这跟"测试失败"是两回事——夹具挂了会让整条测试根本没跑。我去读了完整 traceback
  而不是只看最后一行 `AttributeError`。
- **修正**：载入前先 `sys.modules[name] = module`，失败时清理。
- **教训**：`ERROR` 与 `FAILED` 要分开看。ERROR 是"测试没跑起来"，
  这时候的"红"毫无信息量，不能当成缺陷复现的证据。

### 2.3 上一会话留下的 19.50 基线，本机复现不出来（P0，2026-09-25）

- **AI 的结论**（上一会话，被写进 `docs/ARCHITECTURE-REVIEW.md` 与
  `docs/IMPLEMENTATION-PLAN.md`）：starter 基线 **19.50 / 100**。
- **我的做法**：没有直接把它当基线写进 `EVAL_REPORT.md`，而是先起服务、原样跑一遍。
- **真实结果**：**17.00 / 100**（同一台机器、同一份 starter 代码 `56f7a1f`、
  同为 mock 无 Key 模式）。
- **差异定位**：逐题比对两次的分类分布，差异集中在 `refusal`（F01 两次都不通过，
  F02–F04 有波动）与 `retrieval`（通过的是哪 6 题不完全一样——检索依赖 BM25 排序，
  基线索引只有 53 块）。都是"本来就该不稳定"的类别，不是某一条硬性失败。
- **我怎么处理的**：**两个数字都保留**并写明取舍——以 17.00 为准，因为它带着完整证据链
  （命令 + commit + 原始 `report.json` + 聚合 JSON），而 19.50 只有结论记录。
  这段说明现在就在 `EVAL_REPORT.md` §0 里，没有藏。
- **教训**：上一轮 AI 给的数字，即使被写进了正式文档，也只是一个**未经验证的断言**。
  作业 README 要求"不要默认任何人说的都是对的，包括前同事的交接文档"——
  对 AI 的输出同理，而且 AI 的输出更容易被当成事实，因为它写得像事实。

### 2.4 交接文档的三处不实陈述（P0 复核，缺陷 #15）

- **前同事（`HANDOVER.md`）的说法**：「检索命中率 95%」「测试全部通过」
  「md、txt、html 三种格式都支持」。
- **我实测的**：`retrieval` 6/15；自带 17 条测试确实是绿的，但它是纯冒烟
  （只断 HTTP 200 和字段非空），在总分 17.00 的状态下依然全绿；`loader.py:12`
  只收 `.md`/`.markdown`，`kb_chunks` 只有 53。
- **关键点**：「测试全部通过」**字面上没撒谎**——它只是没告诉你这些测试什么也测不出来。
  这种"真话构成的误导"比直接写错更难发现，只能靠自己去读测试断言。
- **我的处理**：`HANDOVER.md` 原文**不改**（它是评审素材，保留原始错误陈述），
  把三条更正写进 `DEBUG_LOG.md` 缺陷 #15 与仓库根 `README.md`。

### 2.5 AI 写的"aov 分母"测试是拿函数自己的输出算期望（P1，2026-09-25）

- **AI 的建议**：`test_aov_denominator_is_orders` 写成
  `expected = round(got["net_revenue"] / got["orders"], 2)`，
  然后断言 `got["aov"] == expected`。
- **它说的结果**："这条应该会在 starter 上红。"
- **真实结果**：**它在坏代码上也是绿的。** 跑 `tests/defects` 时我注意到
  "26 failed, 2 passed"——顺着查到这条通过了。
- **根因**：starter 的净额与订单数**同时错**（净额少了退款、订单数用了明细行数），
  两个错值相除的比值恰好仍能对上它自己算出的 aov。这不是在断言口径，
  是在断言"函数和自己一致"，而任何函数都和自己一致。
- **我怎么发现的**：**去看了"哪些是绿的"**，而不是只看"红了几条"。
  写缺陷复现测试时，"该红的没红"和"不该红的红了"一样是信号。
- **修正**：期望值写死 `36.36`（题库 M01 的金标），另加一条独立推导——
  净额 ÷ 客单价 应约等于 4311。
- **教训**：**期望值必须来自被测代码之外**。从被测函数里算出来的"期望"，
  测的是自洽性，不是正确性。这条同样适用于 fixture：写死合法，但必须是外部金标。

### 2.6 一条"假绿"的只读测试：失败的 DDL 会毒化 SQLite 连接（P1，2026-09-25）

- **AI 的建议**：D4 的复现测试写成"先跑 `DROP TABLE sales_clean`，再跑
  `UPDATE`/`DELETE`，断言指标不变"。
- **它说的结果**："DROP 会被拒绝，后面的写语句也改不动数据。"
- **真实结果**：`test_write_attempt_leaves_data_unchanged[update]` 与 `[delete]`
  在**完全没修**的 starter 上是**绿的**。
- **根因**：`DROP TABLE` 在共享连接上执行失败之后，**同一个 SQLite 连接会留在
  异常状态，后续写语句静默影响 0 行**。所以"反复查指标都一样"成立，
  但成立的原因不是数据被保护了，而是那条语句压根没执行成功。
- **我怎么发现的**：`test_drop_table_is_refused` 红了（说明 DROP 确实没被拒绝过），
  但同一个模块里依赖它的两条却是绿的——**红与绿自相矛盾**。
  我把 `run_sql` 单独拿出来手跑了一次，看到 `UPDATE ... -> OK rows=0`
  却 `sales_clean 行数: 18628`，才定位到是连接被毒化了。
- **修正**：判据从"语句抛没抛错"改成"**数据有没有变**"，
  并用独立连接隔离每条用例（攻击前查一次、攻击后用新连接再查一次）。
  同时给 `UPDATE`/`DELETE` 加上真的能命中数据的 `WHERE date >= '2026-06-01'`，
  否则"影响 0 行"本身就不说明问题。
- **教训**：**"测试绿了"不是结论，得知道它为什么绿。**
  如果一条测试绿的原因是"它其实什么也没测"，那它比红更危险——
  红的测试至少还在喊。

### 2.7 修好一层之后，另一层的红自己变绿了（P1，2026-09-25）

- **AI 的预测**（规划文档 P1 章的出口标准）："跑公开评测：M 类 6 题应全绿、
  N01 的 valid_sales_rows 绿（kb_docs 待 P2）"。它没有提 `refusal` 类。
- **实测**：`refusal` 从 6.00 涨到 **8.00**，F01 自己绿了。
- **原因**：F01 考的是"9 月营业额是多少"要如实拒答，而区间闸的判据是
  `data_period`，也就是 `MIN(date)/MAX(date)`。基线里日期没规范化，
  这个值是 `{start: "", end: "N/A"}`；P1 把日期规范化之后它是
  `2026-05-01..2026-08-31`，F01 于是直接答出了
  "数据库里只有 2026-05-01 至 2026-08-31 的销售明细"。
- **我自己做的判断**：没有把它当成"顺手多修了一个"写进汇报，而是去核对它
  **是不是真的按规则答的**——重跑、看 `answer_type=refusal`、确认
  `numbers_none_beyond_question` 通过（没编数字）。
  分层修复必然产生连带效应，而连带效应里可能混着"碰巧对上"，
  所以每个意外变绿的题都要单独验一遍。
- **顺带修正了一个假设**：`data` 类 12 分全绿、`hybrid` 里 H05 也全绿，
  比我预估的多。原因是 starter 的 `answerer` 在纯数据问题上的模板本来就够用，
  真正坏的是它拿到的数字。原先以为"编排层要大改才能答对数据题"，
  实际是"数据层修对了，现有模板就能答对一批"——P3 的工作量评估要往下调。

### 2.8 用"挑几个查询试试"来复现 D12 —— 8 个查询只撞上 1 个（P2，2026-09-25）

- **AI 给的测试设计**：`test_hit_docid_owns_chunk` 参数化成 8 个真实查询，
  每个断言"hit.doc_id == chunk_id 的 `#` 前缀"。
- **它说的结果**："这些查询里有一条会红。"
- **真实结果**：8 条里**只有 1 条**红（"Super Souper 周五营业到几点"），
  另外 7 条绿。也就是说这条测试"红着"，但它红得**没有解释力**——
  换个知识库、换个查询集，它可能全绿，而缺陷还在。
- **根因**：`retriever.py:276` 是 `hit.doc_id = ordered[len(hits)].doc_id`，
  要触发它必须让**先处理的 chunk 被"每篇限占一格"跳过**，位置才会错开。
  这是排序与去重交互出来的偶发错位，不是每个查询都会踩到。
- **我怎么发现的**：写完之后我顺手看了**哪些绿**（不只是"红了几条"）。
  7 绿 1 红这个比例不像"稳定复现"，更像"碰运气"。
- **修正**：改成**逐 chunk 遍历**——拿每个 chunk 的前 40 字当查询（保证它自己在候选里），
  断言返回的 doc_id == 该 chunk 的真实归属。每个位置都试一遍，稳定命中。
- **教训**：复现测试的价值在于**红的理由可以被解释**。
  "它在某个查询上红了"不等于"我复现了这个缺陷"。判据要么是确定性的，
  要么得说清触发条件——否则它在隐藏知识库上很可能失效。

### 2.9 修 D13 时顺手挖出一个清单上没有的缺陷（P2，2026-09-25）

- **AI 的规划文档**：P2 列了 D5–D13 九条缺陷，其中 D13 是"先取 top_k 再过滤"。
- **我写 D13 复现测试时的意外**：`test_filter_actually_happens`（断言"这个索引里
  确实存在会被过滤的文档"）**红了**——`as_of=2026-09-01` 下**一篇都没被过滤**。
- **顺着查下去**：`_eligible()` 读的是 `meta.get("status")`，而
  `Document.meta()`（`loader.py:65`）写进去的键叫 **`"state"`**。
  `meta.get("status")` 恒为 `None`，条件 `== "已废止"` 永远不成立。
- **这意味着什么**：**版本过滤从来没生效过**。三篇已废止文档（KB-002/KB-010/KB-012）
  一直当现行用。没有报错、没有告警，只是检索结果里"两份同样的政策"都在。
  V 系版本题（6 分）的地基是空的。
- **我怎么判断出来的**：不是"再读一遍代码"，而是**打印出元数据的键集合**：
  `['doc_id','effective_from',...,'state',...]`——有 `state`、没有 `status`。
  两处单独看都没毛病，只有把两边放在一起才看得出来。
- **修正**：`core/loader.py::Document.meta()` 两个键都提供（`status` 权威，
  `state` 兼容 starter 里已按 `state` 读的地方），并补 5 条测试钉住：
  键一致性、已废止识别、端到端 `filtered` 非空、已废止排第一时不泄漏、
  **归档不被过滤**（归档 ≠ 废止，历史周报仍是资料）。
- **教训**：**"先钉住前提"比"直接断言结果"更容易挖出东西。**
  如果我一开始只写"search 应返回 5 条"，这条会因为"凑得满"而变绿
  （分词坏了、分数恒 0，反而更容易凑满）——正是那条"看起来多余"的前提断言
  （filtered 应该非空）把根因挖出来的。

### 2.10 我自己修出来的回归：后缀匹配把品类词当前缀用（P2，2026-09-25）

- **背景**：R04「三文鱼那次断供供应商赔了多少钱」需要把"三文鱼"认成
  "三文鱼poke"这个对象，才能连到只写 Salmon 的英文邮件 KB-022。
  我给 `aliases._partial_match` 加了"子串判据"。
- **AI（我）的第一版**：`alias[:cut] in lowered or alias[-cut:] in lowered`——
  **前缀和后缀都认**。
- **后果**：R04 绿了，但 **R10 从绿变红**。`照烧三明治` 的后缀 `三明治`
  出现在问句"S04 为什么不卖**吞拿鱼三明治**了"里，于是
  "吞拿鱼三明治"和"照烧三明治"被同时认出来，概念分互相污染。
- **我怎么发现的**：**重跑了 R 系 15 题逐条对金标**，看到 R10 的 top-5 里
  少了 KB-029、多了 KB-060。只看总分（51 → 51）是发现不了的；
  我是按"逐条 PASS/FAIL 列表"核的。
- **修正**：**只认前缀**。理由是这两个词的性质不同：
  `三明治` 是**品类词**（出现在多个别名里，不指向任何一个具体商品），
  `三文鱼` 是**产品词头**（指向明确）。前缀规则天然区分了这两者。
- **教训**：**修 A 的判据很容易顺手把 B 弄坏，而"跑一遍评测并逐条对金标"
  是唯一的发现手段。** 我如果只看总分，这次回归就漏过去了——
  它在总分上被 R04 的 +1 掩盖了。

### 2.11 探针与评测报告不一致，才是最值钱的信号（P2 收尾，2026-09-25）

- **背景**：P2 全部改完之后，`retrieval` 评测是 **15/15**。
  我另写了一个独立进程探针（`build_index` 建索引、逐条对金标）也是 **15/15**。
  两个数字一致——按"总分一致就收工"的习惯，这里就该结束了。
- **我没放过的地方**：去读了评测报告里的**逐题明细**，看到 R10 的 top-5 是
  `['KB-033','KB-060','KB-042','KB-061','KB-022']`——**少了 KB-029**。
  但我本地探针跑同一个查询，top-5 是 `['KB-033','KB-029',...]`。
  **同一个查询、同一份知识库、两个不同结果。**
- **顺着查下去**：在服务进程里 dump 中间量，发现
  `"吞拿鱼三明治" in idx.postings` → **False**，而
  `tokenize("吞拿鱼三明治")` → 正常返回整词。
  **索引里没有这个词，但分词器认得它**——说明 postings 是在分词器还认不出它的时候建的。
  根因：`load_index` 从缓存重建 `BM25Index` 时，`AliasTable` 还没从 JSON 还原，
  `_prepare_tokenizer` 也还没跑，`_build()` 就已经用 jieba 默认词典把
  `吞拿鱼三明治` 切成了 `吞拿鱼`+`三明治`。
- **为什么探针没抓到**：我的探针走的是 **`build_index`（全新建索引）** 那条路，
  永远不碰 `from_json`。**"直接建索引"与"从缓存读"是两条不同的代码路径**，
  而线上服务走的是后者。
- **修正**：把分词结果**冻结进索引**（`Chunk.tokens` 随盘落盘），
  让索引内容只由落盘那一刻决定。更根本的原因是 jieba 的 `add_word` 全局累积——
  实测把顺序调对之后，同一进程连跑四次 `build_index` 仍得到
  2918/2919/2922 三种规模（词典一边加词、切分一边变，正反馈）。
  只修顺序是治标。
- **教训（这条我认为最值钱）**：
  **"两个数字一致"不等于"两边在测同一个东西"。**
  我的探针和评测都在测 `build_index`，所以它们当然一致——
  一致的是**同一个盲区**。真正有用的信号是**评测报告里的逐题明细与本地探针不一致**，
  以及"索引里没有这个词但分词器认得它"这种**内部自相矛盾**。
  只看总分，这次会以"检索 15/15，P2 达标"收尾，而实际上线上跑的是降级索引。

### 2.12 测试夹具污染了整个会话的检索（P2 收尾，2026-09-25）

- **现象**：新写的 `tests/test_index_cache.py` **单独跑 12 passed，
  全量跑 3 failed**，报"S04 为什么不卖吞拿鱼三明治了 的 top-5 里缺少 KB-029：
  `['KB-013']`"。而 `['KB-013']` 正是 `tests/conftest.py` 里那个假检索夹具
  写死的 doc_id。
- **根因**：`client` 夹具（原 `scope="session"`）把 `Retriever.search` 整个换掉、
  **退出时不还原**。于是它一旦跑过，**整个测试会话里所有检索都变成那个固定返回**。
  这是 starter 留下来的测试隔离问题，不是产品代码问题——但它让整份测试报告不可信。
- **我怎么发现的**：报错信息里那个 `['KB-013']` 太眼熟了，
  跟夹具里写死的一模一样；而且"单跑绿、全量红"本身就说明存在跨测试的状态泄漏。
- **修正**：夹具改成函数级、`yield` + `finally` 还原原方法。
- **教训**：**"单跑绿、全量红"一律当成污染处理，不要当成偶发**。
  另外，一个会替换被测类方法的夹具，**必须还原**——否则它在给别人制造假绿/假红。

### 2.13 「两项表现自相矛盾」时，先怀疑观测手段（P3，2026-09-25）

- **背景**：跑 `eval/llm_gateway.py preflight` 验接入，结果
  **P1 失败（假模型一次请求都没收到）而 P12 通过（`llm_mode = live`）**。
- **这两条同时成立在逻辑上说不通**：`llm_mode=live` 说明三个环境变量都读到了，
  那服务就该往注入的地址发请求。要么 P1 的观测有问题，要么我的服务确实没发。
- **AI 会给的"合理解释"**：多半是"服务没重启"或"地址拼接不对"——
  这两个都是常见原因，但这次都不是。
- **我怎么确认的**：不去猜，做一个**能反证的实验**——
  **把假模型停掉，再问一次**。如果服务根本没在调它，回答不会有变化；
  如果确实在调，就该报连接失败。实测返回：

  > `answer_type=refusal`，"模型服务这次没有正常返回（接口返回错误码 502）…"

  **这条 refusal 就是反证**：服务真的在往那个端口发请求。
- **真正的根因**：**预检自己起的假模型没能绑上端口**——我先手动起了一个假模型
  占着 8901，预检再起一个时绑定失败，**但它的提示信息照打"预检假模型已启动"**。
  于是它统计到的请求数是 0，而我的服务一直在正常调那个端口。
  腾出端口重跑，P1 立刻通过，**P1–P14 全绿**。
- **教训**：**"两项表现矛盾"是观测手段可疑的信号，不是被测对象可疑的信号。**
  我如果按"服务没重启"去查，会重启十次也修不好——
  因为问题根本不在被测的一侧。这条和 2.11 是同一类：
  **矛盾本身就是最有价值的线索，别把它解释掉。**

### 2.14 用评测脚本自己的判据写断言，而不是自己重写一遍（P3，2026-09-25）

- **背景**：F01「9 月的营业额是多少」要求 refusal 且不许编数字。
  我写测试时用 `re.findall(r"\d+", answer)` 取数。
- **真实结果**：**测试红了，代码是对的。** 拒答文案里写了
  "数据库里只有 2026-05-01 至 2026-08-31 的销售明细"，
  我的正则把日期里的 `2026`、`05`、`01` 都当成了"编造的数字"。
- **而评测脚本不是这么判的**：`run_eval.py::extract_numbers` 有 11 条
  `_MASK_PATTERNS`，会先把日期、时间、编号、电话号码整段遮掉再取数，
  所以写日期完全安全。
- **修正**：测试改成调 `run_eval_module.extract_numbers(...)`——
  **用评测脚本自己的实现**。`run_eval_module` 夹具本来就有（P0 建的，
  用来复用它的 `normalize_doc`/`check_quote`）。
- **教训**：**"测试比评测严"和"测试比评测松"一样有害。**
  前者会让你去修一个不存在的 bug（这次就是），后者会让真实缺陷漏过去
  （AI_USAGE 2.1 的 `/api/health` 假红是同一类）。
  凡是有官方判据的地方，**直接调它，不要凭理解重写一份**。

### 2.15 「写了兜底逻辑」不等于「兜底会生效」（P3 收尾，2026-09-25）

- **背景**：C04「三文鱼那次断供，供应商最后赔了我们多少钱」的答案在英文邮件 KB-022
  （"credit note of CNY 8,600"），它与中文问句**一个词都不重叠**，句子级打分得 0、
  永远浮不出来。我加了一个 `_value_rescue`：问句要的是"钱"这种形状的值时，
  直接到相关文档里找带该形状值的句子。
- **它说的结果**：我单测调用 `_value_rescue(plan, res, "money")`，返回了
  `[('KB-022', '3. COMMERCIAL SETTLEMENT We will issue a credit note of CNY …')]`——
  **完全正确**。看着没问题。
- **真实结果**：重跑评测，**C04 依然红**，失分项还是 `cite_all: 没有引用 KB-022`。
  一个新写的功能"返回了正确结果"却"在整体里没生效"。
- **我怎么发现的**：逐条对失败题的失分项——C04 报"没引用 KB-022"，
  而我刚刚验证过 `_value_rescue` 返回的就是 KB-022。**这两件事不可能同时为真**，
  矛盾把我引到下游去找。
- **根因**：`_doc_block` 在拿到候选之后按 `doc_score` **降序**排，而 `limit=2`
  意味着只有前两名能进 citations。KB-022 的 `doc_score` 是 7.01（在 hits 里排第四，
  最低），被两个中文文档挤到第三位、被 `limit` 挡掉。
  **兜底逻辑把候选放进了列表，但下游的排序 + 截断把它丢了。**
- **修正**：改成"救回来的候选优先排"。理由写进代码注释：
  检索的文档级分只决定"优先看哪篇"，而"哪篇真的答上了问题"由 `carries()` 判定，
  **后者是更强的信号**。修完 C04 与 T02 一起绿，公开题库 97 → **100**。
- **教训**：**新增一条路径之后，要确认它产出的东西真的走到了出口，而不是停在中间。**
  单元级验证（"这个函数返回对了"）覆盖不到"它的产出在下游被丢弃"这一类问题。
  这和 2.11（缓存路径丢分词）是同一个形状：**局部正确 ≠ 整体生效**。

---



---

## 3. 哪些判断我自己做，不交给 AI

| 判断 | 为什么自己来 |
|---|---|
| **口径以哪一版为准**（KB-001 v3 而非 v2；退款计入净营业额；空 amount 不回填；客单价分母是去重 `order_id`） | 这是 12 分第一关的地基，且知识库里**故意放了过期版本**（KB-002 是 v2）。AI 很容易引到看起来更"完整"的旧版本上。我逐条读了 KB-001 v3 §3/§4/§5 原文再定。 |
| **所有期望值必须独立复算**（18290 而不是 17890；退款行 94 而不是 254） | AI 给的数字看起来都一样可信。我用 `pos.db` 按六条规则顺序复算了一遍，再用结果去核 M01/M02/M04 的五个指标——**能同时对上题库期望值，才说明口径解读是对的**。这一步不能外包。 |
| **缺陷优先级排序** | AI 倾向于"全都修"。72 小时里 P2/P3 占 42/70 分，必须先修影响面最大的（分词 > loader > 缓存键）。排序是资源分配判断，我按评测分类表的失分权重自己排。 |
| **"修复前确实是红的"这个证据要不要认** | AI 会说"这个测试在旧代码上会红"。我只认**真跑出来的输出**：命令 + 完整输出 + commit hash。上面 2.1 和 2.2 就是这么抓出来的。 |
| **refusal 的措辞边界** | S03 的攻击串里含 `drop table sales;`、`sqlite_master`；F02 不能出现员工姓名。**拒答时把攻击内容复述一遍也是失分**——这条规则 AI 不会主动想到（它的默认行为是"礼貌地解释我为什么不能做"）。措辞白名单我自己定。 |
| **提交历史怎么切** | 作业明说「只有一个 finish commit 的仓库直接不看」，且第二关要看"先红后修"的顺序。commit 粒度是我按评审视角设计的，不让 AI 批量提交。 |
| **19.50 还是 17.00 当基线** | 见 2.3。这是"信文档还是信自己复现"的判断，必须是人的判断。 |

---

## 4. 各阶段记录（随时间追加）

| 阶段 | 记录条数 | 条目 |
|---|---|---|
| P0 | 4 | 2.1 假红测试、2.2 模块加载 AttributeError、2.3 基线数字对不上、2.4 交接文档三处不实 |
| P1 | 3 | 2.5 aov 期望值取自被测函数（假绿）、2.6 失败 DDL 毒化连接（假绿）、2.7 修好一层后别的红自己变绿 |
| P2 | 5 | 2.8 "挑几个查询"复现 D12 不可靠、2.9 修 D13 时挖出 `state`/`status` 键名缺陷、2.10 我自己修出的回归（后缀匹配把品类词当前缀）、2.11 探针与评测逐题明细不一致（缓存路径丢分词）、2.12 测试夹具污染整个会话的检索 |
| P3 | 3 | 2.13 「P1 红而 P12 绿」的矛盾——根因在观测手段（预检假模型没绑上端口）、2.14 用评测脚本自己的 `extract_numbers` 写断言（我的正则把日期当成了编造的数字）、2.15 「写了兜底逻辑」不等于「兜底会生效」（`_value_rescue` 的产出被下游 limit 截掉） |
| P4/P5 | 2 | 2.16 评测"跌到 73 分"其实是网络噪声不是回归、2.17 npm 装依赖卡在 postinstall（沙箱里起不了 esbuild 的安装脚本） |


---

### 2.16 评测"跌到 73 分"，重跑就回 100——环境噪声与代码回归必须分开（P4，2026-09-25）

- **现象**：P4 改完前端后重跑公开题库，总分 **73.00**——M/R 两类 21 题全部挂在 `request` 检查上（请求本身失败），而 chat 类全绿。
- **AI 的第一反应**（如果只看分数）："改动把接口弄坏了，回滚排查。"按这个方向会去查 `server.py` 的路由——但 curl 单测 `metrics/summary` 与 `retrieve` 都是 200。
- **矛盾的信号**：`request` 失败集中在评测的**前 21 个请求**，后面的 chat 全过；同时后台正在下载 Chromium（约 500MB），把本机网络吃满了。前 21 个请求撞上拥塞窗口，超时；后 39 个时下载已过高峰。
- **验证**：什么都不改，**只重跑一遍** → 100.00 / 100。再重跑 → 100.00 / 100。
- **教训**：**"分数突然掉"有两种解释：代码回归或环境噪声，必须先做一次能区分两者的实验（原样重跑）再动手。** 与 2.3（基线 19.50 复现不出）同族：不可复现的坏结果和不可复现的好结果一样不可信。这条也符合本仓库一贯的纪律——回归判定交给 `regression.py`（自带重跑），不靠单次印象。

### 2.17 `npm install` 卡死在 esbuild 的 postinstall（P4，2026-09-25）

- **现象**：`npm install` 在 `node_modules/esbuild` 的 postinstall 阶段挂起后失败，日志里是一段 spawn 错误（`error: [Circular *1]`）。
- **AI 给的常见解释**："网络问题，重试即可。"重试两次，同样的地方挂。
- **定位**：postinstall 要 spawn 子进程跑 `esbuild --version` 做校验；本机执行环境禁止这样起子进程。esbuild 的**平台二进制包**（`@esbuild/win32-x64`）是 npm 自己装的普通依赖，postinstall 只是"顺手验证二进制可用"的优化——跳过它不影响运行。
- **修正**：`npm install --ignore-scripts`，装完确认 `node_modules/@esbuild/win32-x64` 存在，`vite build` 一次通过（662 modules，7.3s）。
- **教训**：**包管理器的"安装脚本"不是必经之路。** 现代构建工具链普遍把平台二进制做成 optional dependency + postinstall 校验的双层结构；校验失败 ≠ 工具不可用。先弄清脚本干了什么，再决定绕过方式。

### 2.18 mock 100 分 ≠ live 100 分——"预检通过"与"真实模型作答质量"是两件事（Key 接入，2026-09-26）

- **背景**：拿到真实 DeepSeek Key 后按 `LLM_SETUP.md` §3 三步切换（零代码改动），`/api/health` 确认 `llm_mode=live`，两道冒烟题全对。
- **AI（含我自己的第一反应）会说什么**："预检 P1–P14 全过、mock 模式 55 题全绿，live 应该稳了。"**这个推断错了 12.5 分**——全量跑完是 **87.50 / 100**。
- **实际失分的三类**（`EVAL_REPORT.md` §6 / `DEBUG_LOG.md` #27–#29）：live 引擎 evidence 超尺寸（4 题，**回答本身全对**）、估算数字纪律没传导给模型（H02，模型为了解释"为什么不采用估算"反而把估算写进了回答）、工具循环 4 轮上限撞上真实模型换词检索的"执着"（H06/T02-3）。
- **怎么发现的**：不是靠对答案，是逐题打印失败检查的 expected/actual（`report.json` 每题带 checks），再进 `/api/trace/{trace_id}` 看模型到底调了哪些工具、几轮。H06 的 trace 一眼看穿根因三：4 轮 8 次调用，关键词各不相同，全是合理但无果的检索——模型没错，是预算设计没料到这种行为。
- **教训**：**预检验证的是协议与管线，评测验证的是作答策略，两者不可互相替代。** 假模型的 `normal` 场景两轮就收敛，永远不会暴露"真实模型检索不到会一直试"的回归；同样，evidence 尺寸问题在 mock 里天然不存在（Answerer 走紧凑工具）。"接入完成"的定义应该是**真实模型下全量评测并逐题核对失败检查**，而不是预检绿灯。
